/// <reference types="react" />

// Ensure React JSX global types are available to TypeScript.
// This file lets the compiler find `JSX.IntrinsicElements` when
// `@types/react` is installed (devDependency in your `package.json`).
